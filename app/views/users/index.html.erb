<table class="table table-striped table-sm table-responsive">
<div class="close-button"><%= link_to event_path(id: params[:id]) do %>
<i class="fa-solid fa-x " style="color: #8a8c8f;"></i>
 <% end %></div>
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Username</th>
      <th scope="col">Email</th>
      <th scope="col">Numero Prevendite</th>
    </tr>
  </thead>
  <tbody>
      <% @users.each do |user| %>
    <tr>
        <th scope="row"></th>
        <td><%= link_to user.username, user_path(id: user.id,)  %></td>
        <td><%=user.email %></td>
        <td><%= @presales.where(user_id: user.id).count%></td>
    </tr>
      <% end %>
  </tbody>
</table>

